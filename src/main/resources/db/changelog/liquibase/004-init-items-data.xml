<?xml version="1.0" encoding="UTF-8"?>
<databaseChangeLog
        xmlns="http://www.liquibase.org/xml/ns/dbchangelog"
        xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
        xsi:schemaLocation="http://www.liquibase.org/xml/ns/dbchangelog
        http://www.liquibase.org/xml/ns/dbchangelog/dbchangelog-3.8.xsd">

    <changeSet id="init_items_data" author="roman">
        <preConditions onFail="WARN">
            <not>
                <sqlCheck expectedResult="0">SELECT COUNT(*) FROM items;</sqlCheck>
            </not>
        </preConditions>
        <sql>
            INSERT INTO items (title, description, count, price) values ('title1', 'description1', 1, 101.1);
            INSERT INTO items (title, description, count, price) values ('title2', 'description2', 2, 102.2);
            INSERT INTO items (title, description, count, price) values ('title3', 'description3', 3, 103.3);
            INSERT INTO items (title, description, count, price) values ('title4', 'description4', 4, 104.4);
            INSERT INTO items (title, description, count, price) values ('title5', 'description5', 5, 105.5);
            INSERT INTO orders (status) values ('GATHERING');
            INSERT INTO order_items (order_id, item_id, count) values (1, 1, 1);
        </sql>
        <rollback>
            <sql>
                TRUNCATE TABLE order_items;
                TRUNCATE TABLE orders;
                TRUNCATE TABLE items;
            </sql>
        </rollback>
    </changeSet>
</databaseChangeLog>